window.addEventListener('load', function(){

    /*var titulo = this.document.querySelector('.encabezado__indicador');

    function hoverTitulo(){
        TweenMax.to(titulo, 1, {y: -100});
        
    }
    titulo.addEventListener('mouseenter', hoverTitulo);*/


});
